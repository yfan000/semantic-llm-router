"""\nanalyze_sensitivity.py — Aggregate TTCA sensitivity sweep results into a LaTeX table.\n\nReads router_ttca.csv from each matched results directory, computes per-run metrics,\nand prints a LaTeX tabular with one row per parameter value.\n\nUsage — α sweep:\n    python tests/analyze_sensitivity.py \\\n        --sweep-param alpha \\\n        --results-glob \"results/experiment_*_beta0.0_static\" \\\n        --eval-matrix-glob \"results/experiment_*_beta0.0_static/eval_matrix.csv\"\n\nUsage — β sweep:\n    python tests/analyze_sensitivity.py \\\n        --sweep-param beta \\\n        --results-glob \"results/experiment_*_alpha1.0_beta*_static\"\n\nOutput: prints a LaTeX table to stdout (redirect to a .tex file for inclusion).\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport csv\nimport glob\nimport os\nimport re\nimport sys\nfrom collections import Counter\nfrom statistics import mean\n\n\n# ── Helpers (mirror compare_all.py) ────────────────────────────────────────────\n\ndef _safe_float(v) -> float | None:\n    try:\n        return float(v) if v and str(v).strip() else None\n    except (ValueError, TypeError):\n        return None\n\n\ndef _percentile(vals: list[float], p: float) -> float:\n    if not vals:\n        return 0.0\n    s = sorted(vals)\n    return s[min(int(len(s) * p / 100), len(s) - 1)]\n\n\ndef load_eval_index(path: str) -> dict[tuple[str, str], bool]:\n    \"\"\"Build (req_id, model_id) -> is_correct from eval_matrix.csv.\"\"\"\n    if not path or not os.path.exists(path):\n        return {}\n    index: dict[tuple[str, str], bool] = {}\n    with open(path, newline=\"\", encoding=\"utf-8\") as f:\n        for row in csv.DictReader(f):\n            ic = row.get(\"is_correct\", \"\")\n            if ic in (\"true\", \"false\"):\n                index[(row.get(\"req_id\", \"\"), row.get(\"model_id\", \"\"))] = (ic == \"true\")\n    return index\n\n\n# ── Parse sweep parameter from directory name ───────────────────────────────────────────────\n\ndef parse_param(dirname: str, sweep_param: str) -> float | None:\n    \"\"\"Extract alpha or beta value from directory name like experiment_*_alpha0.5_beta0.0_static.\"\"\"\n    if sweep_param == \"alpha\":\n        m = re.search(r'_alpha([\\d.]+)_', dirname)\n    else:\n        m = re.search(r'_beta([\\d.]+)_', dirname)\n    if m:\n        try:\n            return float(m.group(1))\n        except ValueError:\n            return None\n    return None\n\n\n# ── Per-run metrics ────────────────────────────────────────────────────────────────────────────\n\ndef compute_metrics(results_csv: str, eval_index: dict) -> dict:\n    \"\"\"\n    Load router_ttca.csv and compute aggregate metrics.\n\n    Returns dict with keys:\n        n_total, n_ok, accuracy, ttca_mean_s, ttca_p95_s,\n        cost_mean_usd, top_model, top_model_pct, slo_viol_pct\n    \"\"\"\n    rows_ok: list[dict] = []\n    with open(results_csv, newline=\"\", encoding=\"utf-8\") as f:\n        for row in csv.DictReader(f):\n            if str(row.get(\"status\")) == \"200\":\n                rows_ok.append(row)\n\n    n_total = sum(1 for _ in open(results_csv)) - 1  # subtract header\n    n_ok = len(rows_ok)\n\n    if not rows_ok:\n        return {\"n_total\": n_total, \"n_ok\": 0, \"accuracy\": None}\n\n    # ── Accuracy ──────────────────────────────────────────────────────────────────────────\n    correct: list[bool] = []\n    for r in rows_ok:\n        # 1. Inline gt_correct / gt_scored column\n        gc = r.get(\"gt_correct\") or r.get(\"gt_scored\")\n        if gc in (\"True\", \"true\", \"1\"):\n            correct.append(True)\n            continue\n        if gc in (\"False\", \"false\", \"0\"):\n            correct.append(False)\n            continue\n        # 2. eval_matrix lookup keyed on (req_id, model_winner)\n        key = (r.get(\"req_id\", \"\"), r.get(\"model_winner\", \"\"))\n        if key in eval_index:\n            correct.append(eval_index[key])\n        # 3. No accuracy info — skip (accuracy will be approximate)\n\n    accuracy = mean(correct) if correct else None\n    acc_coverage = len(correct) / n_ok if n_ok else 0\n\n    # ── Latency (TTCA proxy: wall_ms for successful requests) ─────────────────\n    lats_ms = [v for r in rows_ok\n               if (v := _safe_float(r.get(\"actual_latency_ms\") or r.get(\"wall_ms\"))) is not None]\n    ttca_mean_s = mean(lats_ms) / 1000 if lats_ms else None\n    ttca_p95_s  = _percentile(lats_ms, 95) / 1000 if lats_ms else None\n\n    # ── Cost ────────────────────────────────────────────────────────────────────────────────\n    costs = [v for r in rows_ok if (v := _safe_float(r.get(\"charged_usd\"))) is not None]\n    cost_mean = mean(costs) if costs else None\n\n    # ── Model distribution ────────────────────────────────────────────────────────────────────────────\n    winners = Counter(r.get(\"model_winner\", \"?\") for r in rows_ok)\n    top_model, top_cnt = winners.most_common(1)[0] if winners else (\"?\", 0)\n    top_pct = 100.0 * top_cnt / n_ok if n_ok else 0.0\n\n    # ── SLO violations ──────────────────────────────────────────────────────────────────────────────\n    slo_viols = [r for r in rows_ok if str(r.get(\"slo_violated\", \"\")).lower() == \"true\"]\n    slo_viol_pct = 100.0 * len(slo_viols) / n_ok if n_ok else 0.0\n\n    return {\n        \"n_total\":       n_total,\n        \"n_ok\":          n_ok,\n        \"accuracy\":      accuracy,\n        \"acc_coverage\":  acc_coverage,\n        \"ttca_mean_s\":   ttca_mean_s,\n        \"ttca_p95_s\":    ttca_p95_s,\n        \"cost_mean_usd\": cost_mean,\n        \"top_model\":     top_model,\n        \"top_model_pct\": top_pct,\n        \"slo_viol_pct\":  slo_viol_pct,\n        \"routing_dist\":  dict(winners.most_common(3)),\n    }\n\n\n# ── LaTeX output ──────────────────────────────────────────────────────────────────────────────\n\ndef fmt(v, fmt_str: str, suffix: str = \"\", fallback: str = r\"\\todo{X}\") -> str:\n    if v is None:\n        return fallback\n    return f\"{v:{fmt_str}}{suffix}\"\n\n\ndef print_latex_table(rows: list[dict], sweep_param: str) -> None:\n    \"\"\"Print a LaTeX tabular for the sensitivity table.\"\"\"\n\n    if sweep_param == \"alpha\":\n        param_label = r\"$\\alpha$\"\n        caption = (r\"Sensitivity to latency exponent $\\alpha$ \"\n                   r\"(all per-domain $\\alpha$ set uniformly; $\\beta{=}0$, 300 requests). \"\n                   r\"Higher $\\alpha$ penalises slow models more strongly. \"\n                   r\"Accuracy and TTCA are measured on successfully served requests.\")\n        label = \"tab:alpha_sweep\"\n        param_labels = {\n            0.0: r\"0.0 (accuracy-only)\",\n            1.0: r\"1.0 (TTCA, default)\",\n            2.0: r\"2.0 (quadratic)\",\n        }\n    else:\n        param_label = r\"$\\beta$\"\n        caption = (r\"Sensitivity to cost exponent $\\beta$ \"\n                   r\"(per-domain $\\alpha$ at paper defaults: factual=0.3, math=0.7, \"\n                   r\"code=1.0, reasoning=0.7; 300 requests). \"\n                   r\"Cost is in USD per request ($\\sim$10$^{-8}$--10$^{-6}$), so \"\n                   r\"$\\beta$ must be large before cost-awareness materially changes routing.\")\n        label = \"tab:beta_sweep\"\n        param_labels = {\n            0.0: r\"0.0 (cost ignored)\",\n            2.0: r\"2.0 (strong penalty)\",\n        }\n\n    print(r\"\\begin{table}[t]\")\n    print(r\"  \\centering\")\n    print(f\"  \\\\caption{{{caption}}}\")\n    print(f\"  \\\\label{{{label}}}\")\n    print(r\"  \\small\")\n    print(r\"  \\begin{tabular}{lrrrrr}\")\n    print(r\"    \\toprule\")\n    print(f\"    {param_label} & Accuracy & \\\\ttca\\\\ mean & \\\\ttca\\\\ P95 & Cost/req & Top model (\\\\%) \\\\\\\\\")\n    print(r\"    \\midrule\")\n\n    for r in rows:\n        pv      = r[\"param_value\"]\n        metrics = r[\"metrics\"]\n\n        plabel = param_labels.get(pv, f\"{pv:.1f}\")\n\n        acc_str  = fmt(metrics.get(\"accuracy\"),      \".1%\", fallback=r\"\\todo{X\\%}\")\n        mean_str = fmt(metrics.get(\"ttca_mean_s\"),   \".2f\", \"\\\\,s\")\n        p95_str  = fmt(metrics.get(\"ttca_p95_s\"),    \".1f\", \"\\\\,s\")\n        cost_str = fmt(metrics.get(\"cost_mean_usd\"), \".2e\", \"\")\n        if cost_str != r\"\\todo{X}\":\n            cost_str = f\"\\\\${cost_str}\"\n        top_str  = (f\"{metrics['top_model'][:12]} ({metrics['top_model_pct']:.0f}\\\\%)\"\n                    if metrics.get(\"top_model\") else r\"\\todo{X}\")\n\n        print(f\"    {plabel:<28} & {acc_str} & {mean_str} & {p95_str} & {cost_str} & {top_str} \\\\\\\\\")\n\n    print(r\"    \\bottomrule\")\n    print(r\"  \\end{tabular}\")\n    print(r\"\\end{table}\")\n\n\n# ── Console summary ─────────────────────────────────────────────────────────────────────────────────\n\ndef print_console_summary(rows: list[dict], sweep_param: str) -> None:\n    param_label = \"α\" if sweep_param == \"alpha\" else \"β\"\n    header = f\"  {'Value':>6}  {'Accuracy':>9}  {'TTCA mean':>10}  {'TTCA P95':>9}  {'Cost/req':>12}  Top model\"\n    print(\"\\n\" + \"=\" * 80)\n    print(f\"  TTCA {param_label} Sensitivity Summary\")\n    print(\"=\" * 80)\n    print(header)\n    print(\"  \" + \"-\" * 78)\n    for r in rows:\n        pv = r[\"param_value\"]\n        m  = r[\"metrics\"]\n        acc  = f\"{m['accuracy']:.1%}\"    if m.get(\"accuracy\")      is not None else \"  —  \"\n        mean = f\"{m['ttca_mean_s']:.2f}s\" if m.get(\"ttca_mean_s\")  is not None else \"  —  \"\n        p95  = f\"{m['ttca_p95_s']:.1f}s\"  if m.get(\"ttca_p95_s\")   is not None else \"  —  \"\n        cost = f\"${m['cost_mean_usd']:.2e}\" if m.get(\"cost_mean_usd\") is not None else \"  —  \"\n        top  = f\"{m.get('top_model','?')} ({m.get('top_model_pct',0):.0f}%)\"\n        print(f\"  {pv:>6.1f}  {acc:>9}  {mean:>10}  {p95:>9}  {cost:>12}  {top}\")\n        if m.get(\"routing_dist\"):\n            dist_str = \", \".join(f\"{k}:{v}\" for k, v in m[\"routing_dist\"].items())\n            print(f\"{'':>8}  routing: {dist_str}\")\n    print(\"=\" * 80 + \"\\n\")\n\n\n# ── Main ─────────────────────────────────────────────────────────────────────────────────────\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(\n        description=\"Aggregate TTCA sensitivity sweep results into a LaTeX table.\")\n    parser.add_argument(\"--sweep-param\", required=True, choices=[\"alpha\", \"beta\"],\n                        help=\"Which parameter was swept (alpha or beta)\")\n    parser.add_argument(\"--results-glob\", required=True,\n                        help=\"Glob pattern matching results directories \"\n                             \"(e.g. 'results/experiment_*_beta0.0_static')\")\n    parser.add_argument(\"--eval-matrix-glob\", default=None,\n                        help=\"Optional glob for eval_matrix.csv files \"\n                             \"(auto-discovered from results-glob if not set)\")\n    parser.add_argument(\"--output\", default=None,\n                        help=\"Write LaTeX table to this file (default: stdout)\")\n    args = parser.parse_args()\n\n    # ── Discover result directories ─────────────────────────────────────────────────────────────────\n    dirs = sorted(glob.glob(args.results_glob))\n    if not dirs:\n        print(f\"ERROR: no directories matched glob: {args.results_glob}\", file=sys.stderr)\n        sys.exit(1)\n\n    print(f\"Found {len(dirs)} result directories matching '{args.results_glob}'\",\n          file=sys.stderr)\n\n    # ── Load eval_matrix (optional, shared or per-dir) ─────────────────────────────────────────────\n    eval_index: dict[tuple[str, str], bool] = {}\n    if args.eval_matrix_glob:\n        for em_path in glob.glob(args.eval_matrix_glob):\n            idx = load_eval_index(em_path)\n            eval_index.update(idx)\n            print(f\"  Loaded eval_matrix: {em_path} ({len(idx)} entries)\", file=sys.stderr)\n\n    # ── Compute metrics per directory ───────────────────────────────────────────────────────────────\n    rows: list[dict] = []\n    for d in dirs:\n        param_val = parse_param(d, args.sweep_param)\n        if param_val is None:\n            print(f\"  WARNING: could not parse {args.sweep_param} from '{d}' — skipping\",\n                  file=sys.stderr)\n            continue\n\n        # Auto-discover eval_matrix if not provided globally\n        local_eval = os.path.join(d, \"eval_matrix.csv\")\n        if not eval_index and os.path.exists(local_eval):\n            local_idx = load_eval_index(local_eval)\n            print(f\"  Loaded local eval_matrix for {args.sweep_param}={param_val}: \"\n                  f\"{len(local_idx)} entries\", file=sys.stderr)\n        else:\n            local_idx = {}\n\n        combined_idx = {**eval_index, **local_idx}\n\n        results_csv = os.path.join(d, \"router_ttca.csv\")\n        if not os.path.exists(results_csv):\n            print(f\"  WARNING: missing router_ttca.csv in '{d}' — skipping\", file=sys.stderr)\n            continue\n\n        metrics = compute_metrics(results_csv, combined_idx)\n        rows.append({\"param_value\": param_val, \"metrics\": metrics, \"dir\": d})\n        n_ok = metrics[\"n_ok\"]\n        n_total = metrics[\"n_total\"]\n        acc_cov = metrics.get(\"acc_coverage\", 0)\n        print(f\"  {args.sweep_param}={param_val:.1f}: {n_ok}/{n_total} ok, \"\n              f\"accuracy coverage={acc_cov:.0%}\", file=sys.stderr)\n\n    if not rows:\n        print(\"ERROR: no valid results found.\", file=sys.stderr)\n        sys.exit(1)\n\n    rows.sort(key=lambda r: r[\"param_value\"])\n\n    # ── Console summary ─────────────────────────────────────────────────────────────────────────────\n    print_console_summary(rows, args.sweep_param)\n\n    # ── LaTeX table ────────────────────────────────────────────────────────────────────────────────────\n    import io\n    buf = io.StringIO()\n    old_stdout = sys.stdout\n    sys.stdout = buf\n    print_latex_table(rows, args.sweep_param)\n    sys.stdout = old_stdout\n    latex = buf.getvalue()\n\n    if args.output:\n        with open(args.output, \"w\") as f:\n            f.write(latex)\n        print(f\"LaTeX table written to: {args.output}\", file=sys.stderr)\n    else:\n        print(latex)\n\n\nif __name__ == \"__main__\":\n    main()\n